# Separate registration from organization ownership

Registration creates a verified User and session but no Organization or Owner.
Organization setup is a subsequent Organizations use case that establishes
ownership, so an interrupted journey leaves a User without an Organization
rather than a partially created or orphaned Owner.
